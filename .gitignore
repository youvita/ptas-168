node_modules/
.pnpm-store/
.turbo/
dist/
.next/
build/
coverage/

# Backend disk-mode upload dir (R2 in prod). Keep the directory in-repo via
# tracking nothing inside it — gitignore everything except a sentinel file
# if you ever want one.
apps/backend/uploads/

# env files (keep .env.example and .env.production.example tracked)
.env
.env.production
.env.*.local
.env.local

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# editor
.vscode/
.idea/
