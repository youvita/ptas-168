node_modules/
.turbo/
dist/
.next/
build/
coverage/

# Backend disk-mode upload dir (R2 in prod). Keep the directory in-repo via
# tracking nothing inside it — gitignore everything except a sentinel file
# if you ever want one.
apps/backend/uploads/

# env files (keep .env.example tracked inside each app)
.env
.env.*.local
.env.local
apps/backend/.env.development
apps/telegram-bot/.env.development

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# editor
.vscode/
.idea/
infrastructure/docker/cloudflared/creds.json
CLOUDFLARE_TUNNEL_TOKEN
